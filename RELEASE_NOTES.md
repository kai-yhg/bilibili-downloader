# v1.0.2

分 P 多到装不下的时候，界面不再够不着了。

## 新增

- **整页滚动**：内容区支持鼠标滚轮上下滚动，右侧有一根浏览器风格的灰色滚动条，滑块长度随内容比例变化。此前分 P 按钮是每行三个无限往下排的，而窗口尺寸写死为 680x790，分 P 一多就把下面的清晰度、保存目录、文件名、进度条和"开始下载"按钮整体顶出窗口——按钮点不到，只能手动拉伸窗口；分 P 特别多的视频即使用户把窗口拉到最大也未必装得下。实测 60 个分 P 时内容高 1471px，而可视区只有 700px。现在滚动即可到达页面任何位置。
- 解析新视频后视图自动回到顶部。
- 滚轮停在日志框上时滚动日志本身，不会带动整个页面。

窗口顶部的标题栏和"扫码登录"按钮保持固定，不随页面滚动。

## 下载

下载 `bilibili-downloader.exe` 后直接运行。建议放在桌面、下载目录或其他普通可写目录。

## 文件校验

```text
SHA-256: B3C4DCB7AC64A1ED9B57800B396D1D2074712133701025FC7ECF0F8028A5A4A8
```

## 注意

- EXE 未购买代码签名证书，Windows SmartScreen 可能提示未知发布者。
- 扫码登录状态保存在 EXE 同目录的 `bili_login_cookies.json`，请勿分享该文件。
- 本项目不会绕过会员或清晰度权限。

## v1.0.1

修了四个 bug，都属于"出错之后反而更难收拾"那一类。

- **已存在的完整 M4S 会让重试必然失败**：上一版在本地 `.m4s` 已经完整时仍会发出起点落在文件末尾之后的 Range 请求，被 CDN 以 416 拒绝，或者被判定为"CDN 不支持断点续传"，两种结果都是直接报"下载失败"。这恰好打在 README 承诺的排查路径上：MP4Box 合并失败时临时 M4S 会保留，而保留下来的正是两个完整文件，于是用户按提示重试时必定卡死。现在会识别 416 与 `Content-Length` 两种情况并直接判定为已完成。
- **界面事件泵会永久停摆**：此前任何事件处理里抛出的异常都会让 UI 轮询不再重排自身，且打包成无控制台窗口后看不到任何报错，程序表现为整个界面卡死不动。现在单条事件出错会被隔离并写进日志，轮询保证继续。
- **下载会在零字节响应上空转**：CDN 持续返回 206 但不给数据时，旧逻辑会无限重发同一个请求。现在连续三次没有收到新数据即停止并报错。
- **目录与文件名校验是死代码**：旧逻辑在校验之前就做了兜底替换，导致"请选择保存目录并填写文件名"这个提示永远不可能弹出，清空文件名会静默产出 `bilibili_video.mp4`。现在改为先校验用户原始输入。

## v1.0.0

第一个可用版本。

- 解析 B 站 BV 链接与多 P 视频
- 使用 B 站官方扫码登录，显示账号实际可用的清晰度
- 下载 DASH 视频轨与音频轨，支持 Range 分段和备用 CDN
- 使用内置 GPAC MP4Box 无损合并，避免本项目起因中的音画不同步问题
- 下载前选择目录和文件名
- 可选择是否保留原始 M4S
- 单文件 Windows EXE，不需要单独安装 Python 或 MP4Box
